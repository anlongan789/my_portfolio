import 'package:flutter/material.dart';

class MobileTech extends StatelessWidget {
  const MobileTech({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: const Text("Technologies and education"),
      ),
    );
  }
}
